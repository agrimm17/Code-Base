# Project thumbnails

Drop image files here (e.g. `podmd.png`, `my-project.jpg`). They are served at the site root.

**Usage in project data** (server): set `thumbnailUrl` to the path, e.g.:

- `thumbnailUrl: '/thumbnails/podmd.png'`

Use lowercase, no spaces in filenames for consistency.
