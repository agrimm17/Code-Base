# API base URL (used by Contact page; Projects use local data).
# In dev: unset = http://localhost:5001 (run backend with npm start in server/).
# Override to test Vercel API locally, e.g. VITE_API_URL= (same origin, use with vercel dev).
# VITE_API_URL=http://localhost:5001
